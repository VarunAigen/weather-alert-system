# 🚀 How to Test Your Weather App

## ✅ **Easiest Method: Expo Go (No Android Studio Needed!)**

### **Step 1: Install Expo Go**

**On Your Phone:**
- **Android**: Download "Expo Go" from Play Store
- **iOS**: Download "Expo Go" from App Store

### **Step 2: Connect to Your App**

**Option A: Scan QR Code** (Easiest!)
1. Look at your terminal where `npx expo start` is running
2. You'll see a **QR code**
3. **Android**: Open Expo Go app → Scan QR code
4. **iOS**: Open Camera app → Point at QR code → Tap notification

**Option B: Manual URL**
1. Make sure phone and computer are on **same WiFi**
2. In Expo terminal, look for: `exp://192.168.x.x:8081`
3. Open Expo Go → Enter URL manually

### **Step 3: Wait for App to Load**
- First time takes 1-2 minutes
- You'll see "Bundling..." progress
- App will open automatically when ready!

---

## 🐛 **Troubleshooting**

### **"Unable to connect"**
✅ **Fix**: Make sure phone and computer are on same WiFi network

### **"Network response failed"**
✅ **Fix**: 
1. Check backend is running: `http://localhost:8000/docs`
2. Update API URL in `mobile/.env` if needed

### **"Metro bundler error"**
✅ **Fix**: 
```bash
# Clear cache and restart
npx expo start -c
```

### **Lottie animations not showing**
✅ **Fix**: Animations load from internet, make sure phone has data/WiFi

---

## 🎯 **What to Test**

### **1. Home Screen**
- [ ] Dynamic gradient background changes with weather
- [ ] **Lottie animation** shows (sunny, cloudy, rain, etc.)
- [ ] Temperature displays correctly
- [ ] Sunrise/Sunset times show (not "Invalid Date")
- [ ] Wind compass shows direction
- [ ] Weather details grid displays

### **2. Location Features**
- [ ] Tap **+** button to add location
- [ ] Search for a city
- [ ] Location dots appear at top
- [ ] Pull down to refresh

### **3. Other Tabs**
- [ ] **Forecast**: 24-hour chart (collapsible)
- [ ] **Forecast**: 7-day forecast cards
- [ ] **Alerts**: Active weather alerts
- [ ] **Settings**: Toggle dark mode
- [ ] **Settings**: Change user type

### **4. Animations**
- [ ] Weather icon animates (sun rays, rain drops, etc.)
- [ ] Smooth transitions
- [ ] No lag or crashes

---

## 📱 **Expected Behavior**

**Home Screen Should Show:**
```
┌─────────────────────────┐
│  [+]  City Name  [⋮]   │
│       ● ○ ○             │ ← Location dots
├─────────────────────────┤
│                         │
│    [Animated Weather]   │ ← Lottie animation!
│         32°             │
│     Clear Sky           │
│    H:35° L:28°          │
│                         │
│  [Weather Info Grid]    │ ← 2x2 grid
│   Wind | Humidity       │
│   Sun  | (empty)        │
│                         │
└─────────────────────────┘
```

---

## ⚡ **Quick Commands**

**Start App:**
```bash
cd d:\projects\weather\mobile
npx expo start
```

**Clear Cache:**
```bash
npx expo start -c
```

**Restart Backend:**
```bash
cd d:\projects\weather\backend
python -m app.main
```

---

## 🎨 **Features to Show Off**

1. **Dynamic Backgrounds**: Change location to see different gradients
2. **Lottie Animations**: Watch weather icon animate!
3. **Live Data**: Pull to refresh for real-time updates
4. **Dark Mode**: Toggle in Settings
5. **Multi-Location**: Add multiple cities

---

## ✅ **Success Checklist**

- [ ] App loads without errors
- [ ] Weather data displays
- [ ] Lottie animation plays
- [ ] Background gradient looks good
- [ ] Can add new locations
- [ ] Pull-to-refresh works
- [ ] All tabs accessible

**If all checked → Your app is working perfectly!** 🎉

---

## 🆘 **Still Having Issues?**

1. **Check backend**: Visit `http://localhost:8000/docs` in browser
2. **Check Expo**: Look for errors in terminal
3. **Restart everything**:
   - Stop Expo (Ctrl+C)
   - Stop backend (Ctrl+C)
   - Restart both
4. **Clear cache**: `npx expo start -c`

**Your app is ready to test!** 🚀
